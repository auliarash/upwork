[project]
name = "blank-app-template"
version = "0.1.0"
description = "A simple Streamlit app template."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.60.0",
    "pandas",
    "numpy",
    "scikit-learn",
    "matplotlib",
    "plotly",
    "joblib",
    "wordcloud",
    "nltk",
    "Sastrawi",
    "langdetect",
]

[tool.uv]
package = false
